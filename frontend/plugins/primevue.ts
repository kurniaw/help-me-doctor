// PrimeVue is configured via @primevue/nuxt-module in nuxt.config.ts
// This plugin handles any additional PrimeVue setup needed at runtime
export default defineNuxtPlugin(() => {
  // Additional PrimeVue configuration can go here if needed
})
